<Window x:Class="WpfEmbedWinForm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:s="clr-namespace:System;assembly=mscorlib"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfEmbedWinForm"
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="800">
    <StackPanel>
        <TextBlock Text="hello  "/>
        <ItemsControl ItemsSource="{Binding}">
            <ItemsControl.DataContext>
                <x:Array Type="{x:Type s:String}">
                    <s:String>No1</s:String>
                </x:Array>
            </ItemsControl.DataContext>
            <ItemsControl.ItemTemplateSelector>
                <local:AvalonPadTemplateSelector>
                </local:AvalonPadTemplateSelector>
            </ItemsControl.ItemTemplateSelector>
            <!--<ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>-->
        </ItemsControl>
        <!--<local:UserControl1 Width="400" Height="400" Background="Yellow"/>-->
    </StackPanel>
</Window>
